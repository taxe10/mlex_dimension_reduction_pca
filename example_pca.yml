# Example for parameters to excecute

# I/O
io_parameters:
  uid_retrieve: # uid for feature vectors from autoencoder
  data_uris: ["dir_uri1", "dir_uri2", "tiled_uri1"] # either all dir from file system, or all tiled uri
  data_tiled_api_key:
  data_type: 'file' # either "file" or "tiled"
  uid_save:
  output_dir: 'data/output'

model_parameters:
  n_components: 2